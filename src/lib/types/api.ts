import { IMovies } from "./movies";

export interface IResponseNowPlaying {
  dates: {
    maximum: string;
    minimum: string;
  };
  page: number;
  results: IMovies[];
  total_pages: number;
  total_results: number;
}
